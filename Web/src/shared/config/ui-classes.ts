/** Repeated layout/surface class strings — prefer Button size/variant for actions. */

export const uiClasses = {
  navSurface: "rounded-[var(--radius-full)] bg-card p-1.5 shadow-zynd-mid",
  navSurfaceSidebar: "rounded-[var(--radius-full)] bg-card p-1.5 shadow-zynd-mid",
  navLogoLink: "shrink-0 rounded-full bg-card p-1 shadow-zynd-mid",
} as const;
