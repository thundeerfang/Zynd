/** Poll interval while waiting for FinPrim / gateway payment confirmation. */
export const MF_PAYMENT_POLL_MS = 2000;

/** Fast-return confirm window before showing a long-running processing message. */
export const MF_PAYMENT_RETURN_FAST_ATTEMPTS = 90;
